推荐项目清单：

以下方向仅作为参考，不构成限制性要求。参赛者可根据自身能力和兴趣选择合适方向。

基础数据结构与算法

常用数据结构，例如 indexmap、bitmask；
Graph 相关算法库；
Pathfinding 工具库；
moon prove 验证的通用算法库。
工程基础设施与工具链

日志库 + tracing 工具（基于 moonbitlang/async）；
构建工具，类似 n2 / ninja；
模板渲染器；
Stopwatch / benchmark 小工具；
系统能力与运行时框架

Deterministic simulation 框架；
编译器底层基础设施，例如寄存器分配；
面向特定格式的（反）序列化工具，类似 protobuf；
基于 moonbitlang/async 做的并发框架，比如 actor 框架；
应用生态

Markdown to HTML 工具；
简易图表数据生成和预览；
流行游戏引擎的 MoonBit 绑定；
流行数据库的 MoonBit 绑定；

---

## 高潜力方向详细说明

以下选择原则参考比赛评审标准：**解决实际应用问题（25%）+ 完整用户体验（25%）+ 充分利用 MoonBit 语言特性（25%）+ 结合领域知识（25%）**，优先推荐纯 MoonBit 实现、能自然达到万行代码量的方向。

---

### 1. 日志库 + tracing 工具（基于 moonbitlang/async）

| 项目 | 说明 |
|------|------|
| **类型** | 工程基础设施 |
| **比赛对齐** | 日志采集与查询系统（官方推荐） |
| **实现方式** | 纯 MoonBit |

**建设方案（四层架构）：**

- **核心层** — 分级日志（Trace/Debug/Info/Warn/Error/Fatal）、结构化输出（JSON / 纯文本）、日志级别动态过滤
- **上下文层** — Span 管理（进入/退出 span）、span 间父子关系、自动注入请求 ID /  trace ID
- **输出层** — 多目标输出（stdout / 文件 / 网络）、异步写入（基于 `moonbitlang/async`）、日志轮转
- **可观测性集成** — OpenTelemetry 兼容的 span export 接口、与 `moonbitlang/async` 的 task 生命周期自动关联

**MoonBit 语言特性运用：**
- `enum` + 模式匹配处理日志级别和输出目标的路由
- `trait` 定义 `Logger` / `Exporter` 可扩展接口
- `async fn` 实现非阻塞日志写入
- 类型系统保证结构化日志字段的类型安全

**评审亮点：** 每个项目都需要日志，实用满分；异步 + 结构化是加分项。

---

### 2. 模板渲染器

| 项目 | 说明 |
|------|------|
| **类型** | 工程基础设施 / 工具链 |
| **比赛对齐** | 静态网站生成器（官方推荐）的子模块 |
| **实现方式** | 纯 MoonBit |

**建设方案：**

- **解析层** — 模板语法词法分析 + 语法分析（变量插值 `{{ var }}`、条件 `{% if %}`、循环 `{% for %}`、过滤器 `{{ var | upper }}`）
- **编译层** — 将模板 AST 编译为指令序列（指令式 VM 或直接函数生成）
- **运行时层** — 执行编译后的模板，绑定数据上下文，支持模板继承（block/extends）和包含（include）
- **扩展机制** — 自定义过滤器（trait）、自定义标签

**可以扩展为完整的静态网站生成器：**
- Markdown 解析 + 模板渲染 + 主题系统 + 文件监听热重载 + 部署输出

**MoonBit 语言特性运用：**
- `enum` 定义模板 AST 节点（`Text` / `Variable` / `Block` / `ForLoop` / `IfBranch`...）
- 模式匹配实现模板求值器
- `trait` 定义 `Filter` 可扩展接口
- 函数式风格处理不可变的渲染上下文

**评审亮点：** 解析 + 编译 + 运行三阶段完整工程，体量足够；每个网站项目都需要。

---

### 3. 序列化工具（类似 protobuf）

| 项目 | 说明 |
|------|------|
| **类型** | 系统能力与运行时框架 |
| **比赛对齐** | 工程基础设施方向 |
| **实现方式** | 纯 MoonBit |

**核心定位**：一个轻量级的 Schema-driven 序列化框架，定义 IDL（接口定义语言）/ 使用 MoonBit 类型系统注解，自动生成二进制编码/解码。

**建设方案：**

- **Schema 定义层** — 用 MoonBit 类型 + 注解声明数据结构，或设计一个简单的 IDL 解析器
- **编解码生成层** — 编译期或运行时生成高效的二进制序列化/反序列化代码
- **支持类型** — 基本类型、`Option<T>`、`Array[T]`、`Map[K, V]`、嵌套结构体、枚举变体
- **兼容性** — 向后兼容的字段添加/弃用机制（类似 protobuf 的 field number）

**MoonBit 语言特性运用：**
- `enum` + 代数数据类型建模协议消息
- 模式匹配实现编解码器
- `trait` 定义 `Serialize` / `Deserialize` 接口
- 泛型实现对不同容器类型的序列化

**评审亮点：** protobuf 是工业标准，此项目展示对系统编程和协议设计的深入理解；编解码器是 ADT + 模式匹配的"杀手级应用"场景。

---

### 4. Markdown to HTML 工具

| 项目 | 说明 |
|------|------|
| **类型** | 应用生态 |
| **比赛对齐** | 静态网站生成器（官方推荐）的子模块 |
| **实现方式** | 纯 MoonBit |

**建设方案：**

- **解析层** — Markdown 块语法（标题、段落、列表、代码块、引用、表格）+ 行内语法（粗体、斜体、链接、图片、行内代码）
- **AST 层** — 完整的 Markdown 文档树表示
- **渲染层** — AST → HTML 字符串生成，支持扩展渲染器（自定义块/行内渲染）
- **扩展** — GFM 兼容（任务列表、删除线、自动链接、表格）、语法高亮接口

**MoonBit 语言特性运用：**
- `enum` 定义 Markdown AST（`Heading` / `Paragraph` / `CodeBlock` / `List` / `Table`...）
- 模式匹配实现从 token 流到 AST 的转换
- `trait` 实现可插拔的渲染器
- 函数式风格处理解析状态

**评审亮点：** 经典的解析器项目，是静态网站生成器的核心组件，能直观展示 MoonBit 在文本处理领域的优势。

---

### 5. 并发框架（Actor 模型）

| 项目 | 说明 |
|------|------|
| **类型** | 系统能力与运行时框架 |
| **比赛对齐** | 并发框架方向 |
| **实现方式** | 纯 MoonBit，基于 `moonbitlang/async` |

**核心定位**：在 `moonbitlang/async` 之上构建一个 Actor 并发框架，类似 Erlang/OTP 的 actor 模型或 Rust 的 `actix`。

**建设方案：**

- **Actor 核心** — Actor 定义 trait（`Message`、`Handler`）、邮箱（mailbox）实现、消息投递
- **生命周期管理** — Actor 创建、停止、监督（supervisor）树、重启策略
- **通信模式** — 请求-响应（ask）、发送后不管（tell）、广播
- **分布式扩展**（可选） — 远程 actor 通信、序列化消息

**MoonBit 语言特性运用：**
- `trait` + 泛型定义 `Actor[Message]` 和 `Handler[Message, Reply]`
- `enum` 建模消息类型和 actor 状态
- 模式匹配处理不同类型的消息
- `async fn` 实现非阻塞消息处理
- `moonbitlang/async` 提供底层并发原语

**评审亮点：** 高并发是后端核心需求，actor 模型展示对分布式系统设计的理解；纯 MoonBit 实现，不依赖外部运行时。

---

### 6. Graph 算法库 + Pathfinding 工具库

| 项目 | 说明 |
|------|------|
| **类型** | 基础数据结构与算法 |
| **比赛对齐** | 算法库方向 |
| **实现方式** | 纯 MoonBit |

**建设方案：**

- **图表示** — 邻接表 / 邻接矩阵 / 边列表，支持有向/无向/加权
- **基础算法** — BFS、DFS、拓扑排序、连通分量、强连通分量（Tarjan）、最小生成树（Kruskal / Prim）
- **路径查找** — Dijkstra、A\*、Bellman-Ford、Floyd-Warshall、双向 BFS
- **高级算法** — 最大流（Dinic / Edmonds-Karp）、二分图匹配

**MoonBit 语言特性运用：**
- `enum` 表达图搜索状态（`Unvisited` / `Visited` / `Processed`）
- 模式匹配在不同算法实现中处理边界条件
- `trait` 定义 `Graph` 接口，支持不同底层存储
- 函数式风格实现不可变图算法
- 泛型支持不同权重类型

**评审亮点：** 算法实现与 MoonBit 函数式特性天然契合；可配合 `moon prove` 做形式化验证增强。展示扎实的算法功底和对 MoonBit 类型系统的深入运用。

---

### 选型建议总结

| 方向 | 推荐度 | 难度 | 体量 | 语言特性展示 |
|------|--------|------|------|------------|
| 日志库 + tracing | ⭐⭐⭐ | 中 | 大 | ✅ ADT / trait / async |
| 模板渲染器 | ⭐⭐⭐ | 高 | 大 | ✅ ADT / 模式匹配 / trait |
| 序列化工具（protobuf-like） | ⭐⭐⭐ | 高 | 大 | ✅ ADT / 泛型 / trait |
| Markdown to HTML | ⭐⭐⭐ | 中 | 中 | ✅ ADT / 模式匹配 / trait |
| Actor 并发框架 | ⭐⭐ | 高 | 很大 | ✅ trait / async / 泛型 |
| Graph 算法库 | ⭐⭐ | 中 | 大 | ✅ ADT / 泛型 / 模式匹配 |

> 注：如果希望稳妥且有较好产出比，**日志库** 和 **Markdown to HTML** 的难度相对可控且评审价值高；如果希望冲击高分（特等奖/一等奖），**模板渲染器** 或 **序列化工具** 的工程深度和挑战性更足。